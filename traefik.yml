providers:
  file:
    directory: /configs
    watch: true

entryPoints:
  web:
    address: ":80"

  websecure:
    address: ":443"

api:
  insecure: true

log:
  level: DEBUG

http:
  middlewares:
    limit:
      buffering:
        maxResponseBodyBytes: 10000000
        maxRequestBodyBytes: 10000000

    #redirecttohttps:
    #  redirectScheme:
    #    scheme: https
